# Tracking Doc Index

One line per tracking doc. **Doc triage reads this file first** (see `.claude/CLAUDE.md`
§Tracking Documents) — the scope column is the *only* thing triage sees, so it must stay
accurate. If a doc's content grows past what its scope line describes, rewrite the line
(and bump `Updated`) in the same step that grows the doc. A stale scope silently hides the
doc from every future conversation.

ACTIVE docs govern current work. CLOSED docs settle past questions — never deleted, may be
reopened; consult them for cross-reference, but they do not govern current work.

Newest first within each section.

## Active

| Updated | Doc | Scope |
|---|---|---|

| 2026-08-23 | `intnote_trig_reco_eff_sections.md` | **Internal-note sections on TRIGGER efficiency and RECONSTRUCTION efficiency + full note figure sync.** Writes `IntNotes/tex/trigger_efficiency.tex` and `tex/reconstruction_efficiency.tex` from the CODE as ground truth (data tag-and-probe single-mu eps^nc, MC eps_dR correlation correction, pp 2mu4 product vs PbPb mu4 union; 3D pair eps_reco from pp24 Pythia fullsim + PbPb Run-2 placeholder), wires them into the master, and brings `figures/figure_manifest.yaml` + the copy-based sync to 0 stale / 0 missing / 0 orphaned. Also carries a §Inconsistencies list of every tracking-doc-vs-code disagreement found, to elevate to the user. IN PROGRESS. |
| 2026-08-18 | `pp24_crossx_rerun_2026_08.md` | **pp24 cross-section rerun** with (1) the detector-gap fiducial cut applied to BOTH muons (`ParamsSet::single_mu_fiducial_gap_cuts`, replacing the standalone per-muon `q*eta < 2.2`), (2) the one-sided Delta-p/p cut, (3) trigger efficiency = data single-muon mu4 eps^nc per leg x MC-derived 2mu4 eps_dR (no plateau correction, opposite-sign, 7 pair-pT bins = canonical 8 with the last two combined, exponential fit with polynomial then raw-value fallback), (4) reco efficiency from the pp24-condition Pythia fullsim sample instead of the Run-2 placeholder. **DONE 2026-08-18, pipeline ran clean end-to-end.** Deliverables regenerated: `plots/single_b_analysis/pp24/` (5 PNG) + `pp24_pt_150/` (3), `plots/sanity_check_crossx/PP_2024_*`, `plots/mc_data_compr/` (7). **Key numbers:** inclusive eps_reco = 0.6620 Tight / 0.7301 Medium (FIDUCIAL — gap cut on BOTH the truth denominator and the reco numerator, so eps_acc = 0.9133 is NOT folded in and the result is a FIDUCIAL cross-section); correction stages 1761.7 pb raw -> 2601.6 (x1.477 = mean 1/eps_reco) -> 5784.0 (x2.223 = mean 1/eps_trig); eps_dR routing 60/63 cells on the exponential fit, 2 on the polynomial, 1 on the raw bins. **Two bugs found and fixed at source:** (a) the gap windows were rejected on OPEN intervals while the q*eta turn-on bins are half-open `[lo,hi)`, so a muon at exactly q*eta = 2.30 survived the cut with no fitted turn-on -> hard throw; (b) even closed, the JIT Filter compared a float q*eta promoted to double against a double literal, so 2.30f < 2.3 and the edge muon still slipped through — `FiducialGapCutExpr` is now built entirely in float. Both fixes are measure-zero (eps_reco identical before/after). Pipeline stage 5 hardened: ROOT exits 0 after an exception inside the RDF event loop, so freshness + a required-histogram check were added. **CARRIES FORWARD:** pp24 and PbPb signal regions now DIFFER (PbPb still `q*eta < 2.2`) -> do NOT recompute R_AA until PbPb is brought over; one forward eps_dR cell delivers 1.168 > 1, a raw-bin-placeholder artefact escalated to the user under `mc_trigger_efficiency.md` R26. OUT of scope: unfolding, signal acceptance, template fits, PbPb/R_AA. |
| 2026-08-18 | `mc_trigger_efficiency.md` | MC-based trigger efficiency from the trigger-enabled `_July2026` skims (pp24 fullsim → 2mu4, HIJING overlay → mu4): MC singles ε(pT,q·η) vs data T&P, ΔR-binned factorization check, and the ΔR correlation correction ε_ΔR (roadmap Q4, replaces the ε_ΔR≡1 dummy). Rerun 2026-07-14 on both NTP fixes with all 24 pp slices: **pp ε_ΔR^2mu4 plateau 0.9588±0.0094, overlay ε_ΔR^cross 0.8776±0.0257 (ΔR∈[1,4]) — plateau-normalize before use.** **⚠ KEY FINDING (needs user decision): §2 assumption (i) is VIOLATED — the SINGLE-muon efficiency IS ΔR-dependent (ε(ΔR<0.12)/ε(ΔR>1) = 1.20 pp / 1.34 overlay, up to 1.9–2.1 forward) ⇒ pp/2mu4 (product) SAFE, PbPb mu4 UNION weight AT RISK.** Also: MC/data over-efficiency is **barrel-only** (~1.2; endcap ~1.01), plus a genuine MC L1 turn-on saturation at \|η\|>2, pT<6 (not a code bug); trigger-MENU caveat RESOLVED (Dev ⊃ PhysicsP1, convener). **MERGED to master 2026-07-14** (`8edc4fb`). **Round 2 (2026-07-14):** MC muon sample is now **RECO-SEEDED + truth-matched REAL** (`prob>0.5 & \|truth_id\|==13 & IsPrimary`; §3.0/D4 — real HIJING muons KEPT; the overlay reco collection is only 36% real, 64% fake+hadronic) and cut **exactly like data** (fixed a real `fabs(Δp/p)` data/MC mismatch, D5 — **⚠ blast radius: reco-eff / det-response / template-fit MC are now STALE**). Delivered at **BOTH working points**: ε_ΔR^2mu4 = 0.9583±0.0095 (T) / 0.9589±0.0091 (M); ε_ΔR^cross = 0.8426±0.0234 / 0.8407±0.0224 — **WP-independent**. The §2 assumption-(i) violation **survives the truth-match fix** ⇒ genuine L1 correlation, not contamination. **Round 3 (2026-07-16):** dp/p cut is **ONE-SIDED by definition** — data's `fabs` (present since 2022) and the round-2 MC copy of it both fixed (`71fcf1c`); trig-eff rerun (MC + pp24/pbpb23 data refs): **ε_ΔR^2mu4 = 0.9569±0.0094 (T) / 0.9576±0.0090 (M), ε_ΔR^cross = 0.8429±0.0232 (T) / 0.8352±0.0219 (M)** (R9); **rest of the data-side dp/p blast radius still stale** (Remaining Work 3b). Tight/Medium wiring verified NO BUG (mu4 response nearly WP-blind); Medium plots → `medium/` subdirs. **Round 4 (2026-07-20): the r17663 no-overlay skim was RUN (task 51600634) and RESOLVES the R8 outcome tree — the dominant cause of the forward-endcap anomaly is the r16578 tag's trigger CONFIGURATION, not HIJING occupancy** (bin A ε = 0.5672±0.0202, i.e. overlay-like: 16.1σ from pp24, 2.7σ from the overlay; barrel control clean ⇒ the overlay's barrel deficit IS occupancy and the configuration effect is endcap-specific). **The failing STEP is deliberately NOT localized — the earlier "endcap L1 config" attribution is WITHDRAWN (R10**: `HLT_MuonsCB_RoI` is RoI-seeded, so its high value in r17663 argues against an L1-RoI deficit; L1 vs HLT-hypo cannot be separated with our observables). r17663 has a fully separate identity (`FullSimSampleType::noovl`, own dir/label/plot root); pp24 + overlay outputs untouched. Trigger-group question narrowed to the r16578 config and widened to "which step" — **awaiting send**. **Round 5 (2026-07-22, DONE + both reviews PASS):** four user changes — (#1) MC muon sample REVERTED to truth-seeded Pythia-only (overlay HIJING muons EXCLUDED, 99763→95389; pp no-op); (#2) forward q·η bin (−2.4,−2.0) SPLIT into (−2.4,−2.2)+(−2.2,−2.0) (MC + data T&P re-fit); (#3) Step-2 split into **L1 / HLT\|L1 / full-chain** subdirs via a grid RE-SKIM of all 3 samples adding a per-muon `muon_match_L1MU3V` branch (eff(L1)≥eff(chain), eff(HLT\|L1)≤1, closure 0.95–0.98); (#4) Step-3 **pair-η** panels + plateau/fluctuation tables (pp plateau ~0.97–1.00 validates the plateau→1 normalization). **pp uses the FULL sample** (LGD farm v2). Headlines: pp ε_ΔR^2mu4 0.9855/0.9875, overlay ε_ΔR^cross 0.8795/0.8697. Commits `dea8c26`..`ec8ef7d`. Carry-over open (pre-round-5): §2 union-weight decision, trigger-group send, full data-side dp/p cascade, plateau-normalize before crossx; **NEW blast radius: crossx stale on the q·η-split forward-bin trig-eff weight.** **Round 6 (2026-07-28, DONE, both reviews PASS): Step 4 — single-leg ΔR correction `ε_ΔR^single(ΔR)=ε_single(ΔR)/ε_single(ΔR>1)` via leg-level inverse weighting (§3.4), RESOLVING Remaining Work 1 (the §2 union-weight decision): union reformulated `P = ε_ΔR^single·(ε₁+ε₂) − ε₁ε₂·ε_ΔR^cross`. Deliverable overlay (plateau 0.951 T / 0.943 M, small-ΔR rise→R4's 1.34); pp_full = validation only (plateau 0.993≈1, per-cell 0.94–1.01 ⇒ machinery self-consistent; NOT applied to pp — 2mu4 product absorbs it). Both WPs; new `step4_dr_correction_singles/` plot dir. `do_step4` in FillMCTrigEffHists + Step-4 block in plot_mc_trig_eff. Downstream remaining (RW6): plateau-normalize ε_ΔR^single then wire into PbPb crossx union.** **Round 7 (2026-08-03, IN PROGRESS):** (i) **TRUTH FIDUCIAL `truth pT>4 & |truth η|<2.4` is now the DEFAULT sample selection** for every sample and step (§3.0c'; keeps 98.5% of pp, ε 0.7682→0.7722) and ALL plots were regenerated on it — new headlines pp ε_ΔR^2mu4 **0.9817±0.0008**, ε_ΔR^single **0.9911±0.0004**; overlay ε_ΔR^cross **0.8734±0.0183**, ε_ΔR^single **0.9488±0.0085** (Tight). (ii) **ERROR-BAR BUG FOUND AND FIXED (R12)** — the ΔR-correction bars were over-stated by 1.5–3.2× by an independent-error `TH1::Divide` on a ratio whose numerator is a SUBSET of its denominator (χ²/ndf of a constant fit 0.25→1.0); now conditional/binomial-correct incl. the Step-4 leg-leg covariance. Central values unchanged, but **the plateau offset is now 22σ from 1 ⇒ the plateau-normalization systematic must be RE-DERIVED**. (iii) NEW `n_vtx` NTP propagation + §3.5 Step-1 SANITY CHECK (1 reconstructed vertex; |ΔpT|/pT^truth<0.10) to test whether the forward MC≫data anomaly is a bad-muon/pile-up artefact — pp24 fullsim has real pile-up (exactly 1 track-bearing vertex in only 5.0% of events, mean ~4), while the HIJING overlay has exactly 1 in 100% of events so the requirement is a no-op there. (iv) NEW `Analysis/docs/systematic_uncertainties.md` (registry; none existed). Also delivered/in flight: corrected-MC study (SF=ε_data/ε_MC), ΔR-correction plateau ROOT file + fits, and the MC CLOSURE TEST written as the next to-do (RW 8, NOT executed pending user check). **Round 9 (2026-08-11, pp only; PbPb code updated + replotted but NOT refilled; r17663 untouched):** new Step-1 **2D (q·η, pT) single-muon efficiency maps** (binning bit-identical to the data map); `step3_dr_correction/` restructured into **three ΔR ranges × one PNG per pair-pT bin × one subplot per pair-η bin with the per-cell plateau drawn** (the two integrated canvases dropped); **same-sign / opposite-sign separated Step-3/Step-4 fits** under `sign_intgr/` + `sign_sepr/`; and **MC pair-count / cross-section CSV tables** on the 8-bin pair-pT axis (`mc_statistics_pt8bins/`). **★ TWO NEW OPEN, USER-FACING FINDINGS.** **R25:** the Step-3 (both-legs) correction is **NOT charge-blind** — same-sign/opposite-sign = **0.43 at ΔR = 0.025**, dying out by ΔR ≈ 0.175, absent from Step 4; a weight-only numerator (`numraw`) proves it is in the RAW trigger probability, ruling out both an ε_MC-weight artefact and a kinematic-mix artefact ⇒ a genuine charge-dependent two-body correlation (L1 close-by-RoI signature). The sign-integrated nominal ≈ the opposite-sign curve (87 % of pairs), so it **under-corrects same-sign pairs by up to ×2.3 at small ΔR — and the background subtraction is OS − SS**. Applying the sign-dependent correction needs a crossx refill ⇒ user decision. **R26:** the **NOMINAL `expo` fit form cannot describe the densest cell** (χ²/ndf = 48.2; the points dip then overshoot, which a monotone form cannot represent) and `fit_ok` has no χ² term so every badly-fitted cell is published. **BOTH parametric forms fail in a substantial minority of cells** (7–19 of ~71 above χ²/ndf 5, Step-4 tails in the hundreds); `polyu_fixedRp` is NOT globally better (it wins the Step-3 max and the at-limit count 0 vs 34, but loses the Step-4 aggregate, mean 23.4 vs 12.5) ⇒ user decision, most likely a χ² screen in `usable` or per-cell interpolation rather than a form swap. **R27 (2026-08-11, error-bar audit):** the ΔR-correction bars are CORRECT as conditional/binomial STATISTICAL errors (σ_Divide/σ_cond 1.5–1.9; plateau χ²/ndf 1.13 vs 0.39), but they are NOT the total uncertainty — **two correlated terms are unquoted**: the ε_MC parameterization (~1–2 % per leg; leverage ≈0.1 at ΔR→0, ≈0 in the plateau ⇒ ~0.1–0.2 %, i.e. 0.3–0.6× the stat bar at small ΔR) and the per-cell plateau normalization (median **1.53 %**, dropped by `Scale(1/plateau)`); event-level `pass2mu4` sharing is negligible (2.5 % OS / 0.5 % SS multi-pair events). Also: only **40.8 % of same-sign / 66.6 % of opposite-sign** selected pairs fall inside the 8–150 GeV pair-pT axis (the rest are below 8 GeV), and three pre-existing bugs were fixed (a missing pair-pT file-suffix in the fit plotter; `root_file_has_objects()` was a **silent no-op**, so every artefact check in that pipeline had been passing unconditionally; a stale PNG-count literal). **Round 10 (2026-08-12):** `sign_sepr` re-styled for separability (same sign = blue triangles, opposite sign = red circles, markers/error bars one shade darker than their curve) and `step3_dr_fit` split into TWO dR VIEWS — the default `<sign mode>/` is now the FIT DOMAIN dR in [0,1], the reference dR in [0,2] moved to `dR0_2/`, and the same-sign/opposite-sign ratio canvases moved to `ratio/` under each view; the per-cell fit annotation moved into a reserved strip ABOVE each frame, deleting the y-range stretch that used to trade axis inflation against text-over-data. Also fixed a negative kern that had garbled the ε_ΔR defining equation on every INCLUSIVE canvas of every method. **★ Round 11 (2026-08-13) — GAP-WINDOW RERUN, DONE (R29): the fiducial crack window widened `{-0.06,+0.06}` → `{-0.10,+0.06}` (`muon_gap_cuts_acceptance.md` F11a) and that vector is baked into the RDF/JIT selection at FILL time, so the MC chain (Steps 1–4, 3 samples × 2 WPs) and the DATA tag-and-probe (pp24 + PbPb 23/24/25, probe leg) were REFILLED, refit and replotted — every trigger-efficiency product in this doc is current as of 03:2x. Effect is small and benign (guard-failing cells 12→11; inclusive χ²/ndf 10.248→10.278; rejected-cell counts unchanged 32/17). crossx and R_AA did NOT move (the cut is still not in the signal selection). Exposed and fixed: both data trig-eff pipelines only ever ran the TIGHT WP, so the `_medium_wp` tag-and-probe file the Medium MC comparison reads was silently left at the old window — new `pipelines/run_data_trigeff_medium_wp.sh` covers it. Carries a HAND-OFF block for the `mc_trig_eff_closure.md` thread.** **R31 (2026-08-13, side task): the PbPb `forward_qeta_edge_scan/` figure is now DATA-ONLY** (the HIJING overlay row was dropped -- test sample, too few forward muons to inform the edge) and split into TWO PNGs: `_by_year` (mu+/mu- x PbPb 2023/2024/2025, centrality-integrated) and `_centrality` (23+24+25, mu+ + mu-, one panel per `ParamsSet::ctrbins` bin, 3x2). **The adopted forward edge 2.30 is CONFIRMED in PbPb** -- relative to 2.20 it costs |dEps| <= 0.0116 (by year, for +44-47 % probes) and <= 0.0066 (by centrality, 2.28, for +36-38 %), while 2.40 costs 0.038-0.064 (by year) / 0.047-0.058 (by centrality); charge-blind (<= 0.0135) and centrality-blind. **TWO binning defects found and fixed:** 2.25 is NOT a q*eta bin boundary (the curve spanned [2.0,2.26) under a '2.25' label -- R22's pp table header corrected, pp figure regenerated, values unchanged), and the per-centrality q*eta binning is coarser in peripheral bins (184/102/61) so the centrality figure scans only the boundaries common to all six panels (2.20/2.28/2.36/2.40); every label is now written from the achieved bin edge. Caveat: the 2024/2025 `_fine_q_eta_bin` inputs predate the Dp/p fix `71fcf1c` and were NOT refilled (that file name is also read by `TrigEffPlotterPbPb`).  **Round 12 (2026-08-17/18, R32): a THIRD Step-3 plateau mode `nocorr_ptmerge` = the no-plateau-correction fit with the LAST TWO pair-pT bins MERGED into one cell ([72.1,150) GeV, 7x9 = 63 cells), 8-bin axis only, opt-in and suffixed — NOT a new binning (the two filled bins are projected together at the fit stage; ParamsSet::pair_pt_coarse_bins and the histograms are untouched). `expo` + opposite sign + this mode is the TEMPORARY pp24 crossx default, named in code as DrCorrCrossxMethod/Sign/Mode; the Latest Stage carries the HAND-OFF block for `pp24_crossx_rerun_2026_08.md`. It buys COVERAGE, not precision: N_eff only x1.06-1.48, but the 2-3 cells that delivered NO correction are gone and the polyu eps_dR = 2.74 artefact does not survive (merged max 1.24). Regression-checked byte-identical on the un-merged modes. **Two OPEN user decisions (both R26): a positive-amplitude/chi2 screen for `fit_ok` (the new default series has one accepted-but-unusable forward cell, C = 0.490 +- 0.716, delivered via the raw placeholder as 1.168 > 1), and whether the minimum-points threshold should reject the one unconstrained overlay fit the merge creates.**|

| 2026-08-24 | `mc_trig_eff_closure.md` | **MC closure of the pp24 2mu4 trigger correction** (executes `mc_trigger_efficiency.md` Remaining Work 8). MC opposite-sign pairs passing 2mu4, weighted by `1/(ε(1)·ε(2)·ε_ΔR(ΔR))`, must reproduce the all-pairs (no-trigger-requirement) MC spectrum differentially in pair p_T inside pair-η bins. **ROUND 2 (2026-08-18, DONE) changed what is applied and what is drawn:** the applied single-muon mu4 efficiency is now **ε_MC**, not the data tag-and-probe ε^nc, so the test is SELF-CONTAINED and its residual is the ΔR correction alone (the data-ε numerator is kept as a printed diagnostic; their ratio is still ⟨r₁·r₂⟩, the MC/data over-efficiency). **TWO ε_ΔR variants, one plot subdirectory each:** `no_plateau_correction/` (un-merged 8 pair-pT cells, exponential and polynomial fits as two separate corrected series, 3 lines) and `no_plateau_correction_last2ptbins_merged/` (exactly what the pp24 cross-section applies — last two pair-pT bins merged, ONE corrected series from the expo→polyu→raw-bin cascade, 2 lines, built by `Utilities/DrCorrectionCrossxEvaluator.h` itself so the closure tests the delivered object, not a copy). **★ HEADLINE (R4): the correction machinery CLOSES — C(ε_MC) = 0.9940 (all opposite-sign) / 0.9888 (single-b signal region) inclusively, Tight; Medium 0.9955; 4-bin 0.9927; and the merged cascade agrees with the un-merged exponential to 1e-5.** The as-applied data-ε diagnostic still reads 1.2699 (all-OS) — entirely ⟨r₁·r₂⟩ (R1, round 1), not a defect of the correction. **★ OPEN (R5, was R1b): η^pair ∈ [−2.4,−2.0) × p_T^pair > 72 GeV closes at 0.52–0.53 against a mirror bin at 1.08–1.13, survives the merge, and in the MERGED variant is the ONE cell the cross-section corrects with the TEMPORARY raw-bin placeholder — delivering ε_ΔR up to 2.157 where the physics requires ≤ 1** (both fits rejected there: C = 0.490 ± 0.716 / 0.4905 ± 0.400). Ties R1b to parent-doc R26 / Remaining Work 0, not only to the forward-negative ε-map thread. Caveat: 'closes to ~1 %' is INCLUSIVE — individual cells sit 16–18σ from 1, so a trigger systematic from this residual must be per cell. **ROUND 3 (2026-08-24, figures only, no applied number changed to 6 digits):** the ratio pad's y-range is now SHARED between the two PNGs of a variant while the spectrum pad stays per figure (cross sections of two different samples, D6), and the ratio frame now CONTAINS EVERY DRAWN POINT instead of a 2-98 percentile window (D7) -- that window had hidden the R5 forward-negative cell (C = 0.534) from the ratio pad while its marker was drawn in the spectrum pad above. Round-1 (data-ε) PNGs/ROOT/logs quarantined under `superseded_round1_data_eps/`. Pb+Pb OUT of scope (union weight + overlay production in flight); same-sign excluded by the user. |
| 2026-08-13 | `muon_gap_cuts_acceptance.md` | **Detector-gap fiducial cut on single muons + acceptance efficiency ε_acc. CUT SET FIXED (F10/F11a): `ParamsSet::single_mu_fiducial_gap_cuts = {{-1.20,-1.05},{-0.10,+0.06},{2.30,2.40}}` in q·η — crack window ASYMMETRIC (widened to -0.10 on 2026-08-12; the depletion is one-sided, N/mirror = 0.51 pp / 0.58 PbPb over (-0.10,0), while every slice from -1.0 to -0.2 is ENRICHED). Costs 3.79 % pp / 5.60 % PbPb muons (7.4 % / 10.9 % of pairs). **THE VECTOR IS NO LONGER INERT (F11b): live in the trigger-efficiency chain since 2026-08-04 — FillMCTrigEffHists.cxx Steps 1-4 and the data tag-and-probe PROBE leg (RDFBasedHistFillingPP.cxx:172, PbPb.cxx:342) — so the 2026-08-12 widening leaves the MC + data trigger efficiencies STALE and needing a refit, NOT done here (sibling session's `mc_trigger_efficiency.md` owns that pipeline).** Deferred pending go-ahead: wire into the SIGNAL selection (13 call sites, full `signal_selection_change_impact.md` blast radius), reco-eff / ntuple processing / template fits, and building ε_acc itself.** Delivers (1) the single-muon q·η spectrum after the nominal NTuple-processing selection for pp24 and PbPb 23+24+25 combined (centrality-integrated and centrality-binned, 0–10 merged), and (2) the inventory of gap cuts already in the code. **Key findings: THREE coexisting "gap" definitions** — `ParamsSet::eta_gap_cut1=0.135` (|η|, charge-independent) + `charge_eta_gap_cuts={{0.56,0.67},{1.064,1.29},{-1.29,-1.12}}` (q·η) applied by `PassSingleMuonGapCut`, which today is **NOT in the signal selection** (only fills diagnostic `_wgapcut` histograms); a commented-out `eta_gap_cut2={1.05,1.29}` for pT<6; and the *different* holes in `CommonEffcyConfig::q_eta_proj_ranges_fine_excl_gap` (q·η ∈ (−1.3,−0.9), (−0.1,0.1), (1.0,1.3)) which only trigger a 2D trig-eff fallback. Legacy pre-RDF code reuses the SAME ParamsSet values (no independent legacy set). Also: the 2026-06-23 single-muon trees were stale on the one-sided Δp/p fix (`71fcf1c`) and were regenerated. **★ TRUTH q·η REFERENCE ADDED 2026-08-13 (F12), plot set `muon_gap_cuts/truth_q_eta/` from `plot_muon_truth_q_eta_spectrum.cxx`: truth muons in the truth fiducial only (`truth_pt>4 && |truth_eta|<2.4`, NO reco match / working point / trigger), pp24 Pythia fullsim FULL (17.1 M muons) + HIJING overlay PbPb23 (108 k, input REGENERATED — the on-disk copy predated the HIJING-leak fix `8c917b4` and the isospin/`ev_weight` fix `216f750`). RESULT: the barrel/endcap dip, the η≈0 crack and the one-sided forward collapse are **100 % detector** — truth is featureless at all three (1.036 / 1.007 / 1.005 of plateau, where the data sit at 0.32 / deep / 0.094), so the truth spectrum is the valid ε_acc denominator. **ε_acc = 0.9133 (pp) / 0.9121 (overlay)** — the cut costs **8.67 % of TRUE muons vs only 3.79 % of reconstructed ones (×2.3)**, so ε_acc must NOT be built from the data fractions. WP-independent by construction (registry §8). Also F13: the opaque TLegend boxes that covered curve segments in the Pb+Pb new-gap-cut figures are fixed (canvas-level key + entry-count-sized, auto-placed legends); both WPs regenerated, `old_gap_cuts/` deliberately untouched.** Side flags: cross-year FCal scaling is documented but **never implemented in code**; muon-level `ev_centrality` bypasses the PbPb25 pair-level centrality recalculation. |
| 2026-08-04 | `pp_trig_eff_highpt_jump.md` | **REOPENED 2026-08-04 — ACTIVE, BLOCKED ON A USER DECISION.** Owns the pair trigger-efficiency weight pathology at high pair pT. (1) CLOSED 2026-06-21: lambda-built turn-on TF1s returned 0 above the [4,60] fit range on read-back -> eps floored to 0.01 -> pp 2mu4 PRODUCT weight blew up (corrected/raw x13-88 above ~58 GeV); fixed at source (fits rebuilt as TFormula strings so the formula persists; lookup Evals the continuous function at exact pt with min(.,1) cap); pp+PbPb re-fit, full crossx/R_AA rerun, chain-wide audit clean. (2) **NEW, LIVE:** the OTHER branch of the same lookup — `EvaluateSingleMuonEffcyPtFitted` returns the -1.0 'no efficiency' sentinel when a muon's q*eta is in a GAP of `q_eta_proj_ranges_fine_excl_gap` ([-1.3,-0.9], [-0.1,0.1], [1.0,1.3]) AND the 2D fallback bin is empty (high pT) -> `w_trig = 0` -> **the pair is SILENTLY DROPPED from every trigger-corrected histogram**, so corrected/uncorrected falls BELOW 1 (impossible for a 1/eps correction): pp24 down to 0.37 in pair eta ~ +-1.2 above ~50 GeV (7/132 bins), PbPb reaches exactly 0. Contaminates the NOMINAL crossx and R_AA, not just sanity plots (~63% loss in the worst pp bin). Exposed 2026-08-04 by the crossx common log-y range change (`raa_from_rdf_crossx.md`), which removed the `ymax*1e-5` floor that had been hiding those bins. Root cause traced + numerically confirmed twice; **fix NOT applied — 4 candidate approaches (nearest-fitted-bin / coarser 2D fallback / drop-consistently / exclude the gap from the fiducial region) differ in PHYSICS, so the choice is the user's**; fixing needs a crossx refill pp + PbPb 23/24/25 + full replot. |
| 2026-08-04 | `raa_from_rdf_crossx.md` | task_06: R_AA from RDF crossx. REOPENED 2026-06-19 — PbPb 2024 lumi corrected 1.59663→0.85112 nb⁻¹ (old GRL failed to exclude runs <489703; event counts were already correct, lumi-only fix; combined R_AA ×1.17), R_AA equation rewritten to common notation (n_AA raw yield, explicit 1/N_evt), y-title relabel. **REOPENED 2026-08-04 — crossx subplot log-y range:** the `_pt_150` plots looked truncated at high pair-pT because they were STALE (no pipeline stage ever passed `use_pt_bins_150=true`; pp last written 2026-04-17), NOT cropped — ROOT's log autoscale crops no marker (0/36 panel-cases). Real cropping did exist in the two auxiliary macros' hard `SetMinimum(ymax*1e-5)` floor (spectra span 2.3e5 within one pair-η panel). New `Utilities/CommonLogYRange.h` gives every subplot of a PNG ONE data-derived log-y range (floor = global min positive content / 3) so no point can fall off-frame; wired into `SingleBCrossxPlotterBase` (pp+PbPb) and both aux macros; pipelines now regenerate the `_pt_150` dirs alongside the nominal. Also fixed **pre-existing breakage: `plot_crossx_trig_corr_sanity.C` called a removed `DatasetTriggerMap::Get(yr,int)` API and did not compile, so Stage 7 of BOTH crossx pipelines was a silent no-op.** All 98 crossx PNGs regenerated (plotting-only; no RDF refill). |
| 2026-07-14 | `pythia_fullsim_pp24_full_sample_skim.md` | **Pythia fullsim pp24 FULL sample** (`_pdf` production 803015–803020, r16578; 9.87 M events, **pp beam only**). Test run PASSED (branch list identical to the test sample; truth provenance, muon SFs, trigger sim all present; StoreAllEvents verified). 6 grid tasks submitted. **Design D1: LGD-ONLY — the ~280 GB of NTUP (measured 30.6 kB/evt) is never hadded onto GPFS; it is read from a BNL-OSG2_LOCALGROUPDISK symlink farm via a TChain glob** (the future overlay full sample would be ~20 TB). **D2: pp-conditions fullsim is pp-ONLY with isospin weight 1** — the Pb 4:6:6:9 ratio belongs to the PbPb overlay; defaults were backwards and `nominal_beam_ratio[pp]=4/25` was applied to single-beam runs (cancels in all ratios — it is slice-independent — but 6.25× wrong in an absolute σ ⇒ **overlay crossx STALE, rerun pending**). One switch `isTestSample` (default false = FULL) now drives input dir + AMI dir + isospin. **D3: AMI provenance guard** — AMI files are keyed by beam+slice only, so the full sample would have been silently weighted with the TEST sample's σ·ε (slice-dependent, ratio 0.96–1.55 ⇒ cancels NOWHERE); now `expected_ami_dsids` + a hard throw. Also: GPFS crunch → 292 GB migrated to LGD (132.8 GB freed; resume script for the rest). |
| 2026-07-08 | `tight_wp_default_change.md` | Medium→TIGHT default muon working point (pp & PbPb, all pipelines). DONE & CERTIFIED 2026-07-08: tight trig-eff turn-on, tight reco-eff, tight crossx/R_AA reruns, both reviewers PASS. WP-site registry = `Analysis/docs/muon_wp_registry.md`. Open item: pp-tight reco-eff (interim Medium Fig.31 vs peripheral-PbPb-tight — user decision). |
| 2026-07-08 | `analysis_roadmap_2026_06.md` | Analysis roadmap (2026-06-10, living doc): IntNote readiness per section, missing inputs that must come from the user, steps capable-but-unfinished, steps blocked on prerequisites, full analysis chain with dummies. Per-task agent instructions in `Analysis/docs/roadmap_tasks/`. |
| 2026-07-07 | `tf_upfront_bkg_reduction.md` | **Sub-doc E of `low_mass_dimuon_template_fit.md`.** ACTIVE thread: reduce hadronic/fake background UPFRONT via tighter selection instead of fitting it — muon working points (χ² recommendation; `Analysis/docs/references/muon_working_points.md`), \|d0\| discrimination, Δp/p distribution (4-way real-HF / real-prompt / hadronic / fake). |
| 2026-07-07 | `tf_bkg_composition_normalization.md` | **Sub-doc C of `low_mass_dimuon_template_fit.md`.** Background composition; data/Pythia normalization (nb→pb unit fix); provenance classifier; tight-vs-medium WP comparison; extended-mass control region; V1 charge and V2 near-side charge-symmetry checks. |
| 2026-07-07 | `low_mass_dimuon_template_fit.md` | **UMBRELLA + authoritative Physics Procedure + INDEX** for the low-mass (0–4 GeV) background-subtraction program that REPLACES provisional OS−SS in crossx/R_AA. Nominal method = OS−SS + MC(S+G) fit + §3h resonance templates (combined-fit/k abandoned). Factorized 2026-07-06 into sub-docs A–E (`tf_*.md`). **62 kB — read its Physics Procedure + index sections, then only the sub-doc owning your thread.** |
| 2026-07-06 | `tf_nominal_fit_build.md` | **Sub-doc A of `low_mass_dimuon_template_fit.md`.** CURRENT NOMINAL method + fit build: OS−SS + MC(S+G) + resonance templates, reco-level correction ordering, signal acceptance, wiring N_sig into crossx/R_AA. LIVE fitter→R_AA status. |
| 2026-07-06 | `tf_k_factor_mixed_event.md` | **Sub-doc B of `low_mass_dimuon_template_fit.md`.** OS→SS factor k (§5a/5b), mixed-event / ScrambGen T_mix, and the ABANDONED combined OS+SS fit (retained as a systematic and as history). |
| 2026-07-06 | `tf_dpop_fake_muon_program.md` | **Sub-doc D of `low_mass_dimuon_template_fit.md`.** Δp/p fake-muon FIT-AND-SUBTRACT yield program (steps D0–D5). |
| 2026-06-22 | `analysis_status_summary.md` | Current status of all analysis steps across all PbPb years and data types: which steps have been rerun with the May 2026 skim and which are stale. |
| 2026-06-17 | `academic_writing_workflow.md` | Academic writing production chain (rigor + auto-sync): building the G1–G7 gates, the new `/review-note` `/review-paper` `/verify-citations` `/sync-note-figures` `/check-note-sync` `/compile-note` commands and agents, the ARS-plugin-not-installed decision, copy-based figure sync. Tooling/workflow, not physics — authoritative reference is its Workflow Specification section. |

## Closed

| Updated | Doc | Scope |
|---|---|---|

| 2026-07-14 | `hijing_overlay_det_response_band.md` | **CLOSED.** (1) m_μμ^reco=2·m_μ band in the HIJING-overlay det-response matrix → the ΔR<0.05 fallback + non-exclusive barcode path let two truth muons of a collinear pair claim the SAME reco muon; fixed, and the **ΔR fallback DELETED entirely** (it always overestimates pair reco eff for ΔR<0.05) → matching is now pure prob>0.5 exclusive barcode. Also fixed: fullsim global pair-tree double-fill; `hijing_overlay_pp24`→`pbpb23` naming. (2) **r17618 vs r17662 = SAME 10k events but two different digi+reco passes** (`digiSteeringConf` shifts the tracking RNG); nothing physical differs (FCal_Et bit-identical, detector response KS p=0.75) ⇒ BOTH give correct reco eff + det response. Barcode collision causes NO underestimate; spurious-HIJING-match ruled out. (3) **Pythia truth index guard: found & fixed a real leak** (empty Pythia-Geant4 block in 1.17% of events let the HIJING generator block in) → now PERFECT closure (identical flavor/origin/parent-group for all 6271 pairs, 15800=15800 truth muons). **r17618 SAFE for the full sample.** On branch `mc-trigger-efficiency` (unmerged). |
| 2026-07-14 | `hijing_overlay_reco_effcy_investigation.md` | **CLOSED 2026-07-14 (was PARKED).** Why the r17618 HIJING-overlay reco efficiency looked unphysically low (~10-20%). RESOLVED: Bug #1 (index mapping) was the real killer; Bug #3 (HIJING truth muons in the denominator) fixed by the Pythia truth index guard; the residual ~14%-below-pp deficit is PHYSICAL (dense-environment loss, largest at low pT), method- and sample-independent. **Its two remaining open items are now SETTLED in `hijing_overlay_det_response_band.md`:** (a) the ad-hoc ΔR<0.05 fallback it introduced as "Bug #2 fix" was **DELETED entirely** (it overestimates pair reco eff for ΔR<0.05); matching is pure prob>0.5 exclusive barcode. (b) the r17618-vs-r17662 question is closed — the barcode collision causes NO reco-eff bias, and the truth index guard had a real HIJING leak (now fixed). Do NOT re-introduce a geometric/ΔR fallback. |
| 2026-07-14 | `hijing_overlay_truth_barcode_duplicate_investigation.md` | Truth-record and barcode-duplicate behaviour across overlay r-tags r17618 / r17662 / r17663. Established r17618 (full HIJING truth) as the production config; Pythia and HIJING both number barcodes from 1, so they COLLIDE on 1..N. **⚠ CONTAINS A CORRECTED CLAIM (2026-07-13): its statement that r17618 and r17662 have "no common eventNumbers / different EVNT inputs" is WRONG** — that came from early PARTIAL LOCAL skims reading different file subsets; on the full grid skims they are the SAME 10 000 events (100% eventNumber overlap, identical Pythia truth, identical overlaid HIJING). They differ only in the digi+reco pass (`digiSteeringConf`). Per-event r17618↔r17662 comparison IS possible — see `hijing_overlay_det_response_band.md` Step 8/9, which used it to find and fix a real HIJING leak in the Pythia truth index guard. |
| 2026-07-10 | `mc_trigger_info_skim.md` | **CONCLUDED: Run-3 fullsim MC DOES carry trigger simulation** — AMI steering `doRDO_TRIG`+`doTRIGtoALL` on r16578 (pp24 fullsim) and r17618/r17662 (HIJING overlay), confirmed in the AODs; `mu4`/`2mu4`/`mu4_mu4noL1` present and unprescaled (r15970 is only an AODMerge tag). Trigger enabled by default for all Run-3 fullsim/overlay MC via an explicit `mc_has_trigger_sim` gate (`434de17`); **caught the `StoreAllEvents=false` trap that silently trigger-OR-filters the MC** (would have destroyed the reco/trigger-efficiency denominators) — MC now keeps all events. Re-skimmed `July2026.v1`: 31/31 tasks, all 10 000 entries, 24/24 pp files bit-identical to backups outside trigger branches. **MC/data: f(2mu4\|mu4)=1.23, f(mu4_mu4noL1\|mu4)=1.02 ⇒ MC over-estimates the SECOND muon leg (L1 RoI×HLT) efficiency by ~20%** — systematic for the MC ΔR correction (ε_trig stays data-driven). Also derived the pp24 prescale structure (PS(mu4)=9.31, PS(L1_MU3V)=3.60, PS(2mu4)=1) matching lumi bookkeeping to 0.3%. New: `run_pythia_fullsim/grid_sub.sh`, `grid_monitor.sh --mode fullsim_pp`. Roadmap Q4 prerequisite "unbiased trigger decision in MC" **satisfied** → successor `mc_trigger_efficiency.md`. Open: `Δp/p` missing `fabs` in `PythiaFullSimExtras.c:41`; role of the `_pdf` production 803015–803020. |
| 2026-06-22 | `remove_dr_cut_signal_selection.md` | Removed the ΔR>0.05 cut from the single-b dimuon signal selection (data + truth + fullsim defs); no-ΔR-cut spectra became the interim nominal; full downstream rerun (data crossx pp + pbpb 23/24/25, pythia truth acceptance, all crossx/R_AA/acceptance/cutflow plots). Blast radius per `Analysis/docs/signal_selection_change_impact.md`. |
| 2026-06-20 | `reco_eff_placeholder_run2.md` | Temporary Run 2 single-muon reco-efficiency placeholder (ε₁·ε₂ proxy) so crossx/R_AA run end-to-end while Run 3 reco-eff MC is unavailable. PbPb = colleague's exact Run 2 Medium TF1 fits (F.2); pp = HF-muon note HION-2019-58 Fig.31, peripheral PbPb as fallback. Follow-ups Q1+Q2 DONE 2026-06-16: Q1 PbPb genuine differential dσ/dp_T = (1/L)·dN (nb/GeV, `differential_crossx` dir; T_AA-weighted kept as R_AA input); Q2 reco folded into the pp generic weight (`generic_weight_col=w_reco_trig`). **Standing INVARIANT from this doc: any pp efficiency / detector-response / unfolding change must be reflected in the MC-data comparison.** That is Stage 8 of `pipelines/pipeline_pp_crossx.sh` and is on by default, so a full pipeline run satisfies it automatically; the invariant only bites when the stage is bypassed (`SKIP_MC_DATA_COMPR=1`) or the change is made out-of-band (reco-eff ROOT file / hist-filling weight edited, then only crossx replotted). Open follow-ups: pt_150 differential crossx; proper 3D pair ε_reco(pair pT, pair η, ΔR) when MC lands. |
| 2026-06-15 | `kb_building.md` | Built the physics/literature knowledge base under `.claude/kb/` plus the `/kb-build` and `/kb-review` tooling — all 4 roadmap steps DONE; 12-source bulk build completed and review-validated; further sources go through `/kb-build` ADD mode. Infrastructure, not a measurement — authoritative reference is `.claude/kb/KB_BUILDING_GUIDE.md` (relevance-first, PRIMARY vs SUPPORTIVE, URL/PDF rule, summary template, no-hallucination, dedup + knowledge graph, self-sync). |
| 2026-06-11 | `pp_pipeline_implementation.md` | Built the two end-to-end pp24 pipelines (`pipeline_pp_crossx.sh`, `pipeline_pp_trig_eff.sh`) mirroring the PbPb structure, with source modifications, pp17 legacy cleanup, and consistency fixes. Key pp-vs-PbPb difference: pp has no event-selection stage (no ZDC, no nTrk_HITight, no FCal centrality). |
| 2026-06-11 | `mu4_trig_effcy_implementation.md` | Implemented the full single-muon mu4 trigger-efficiency chain: no-correlation ε_mu4(pT, q·η), ΔR-dependent correlation corrections ε_ΔR(ΔR) for single-muon and cross term, and the PP24 2mu4 efficiency via analogous inverse weighting. Contains the authoritative Physics Procedure for mu4 union vs 2mu4 product weighting. |
| 2026-06-10 | `analysis_roadmap_buildout.md` | Companion checklist for `analysis_roadmap_2026_06.md`: the 2026-06-10 strategic task that read the Run 2 dimuon internal note (ATL-COM-PHYS-2021-1094) into the KB and produced the roadmap doc + per-step task files. |
| 2026-06-09 | `new_skim_evsel_pipeline_rerun.md` | Integrated the PbPb event-selection stage into both pipeline scripts (crossx and trig_eff), fixed the PbPb23 file map (added part4, 8.8M entries), and reran both pipelines for all 3 years to refresh all outputs with the May 2026 skim. |
| 2026-06-09 | `localgroupdisk_migration.md` | Migrated ~1.4 TB (pythia_truth, powheg_fullsim, dimuon_data) from personal pnfs/dCache to RSE `BNL-OSG2_LOCALGROUPDISK`, then repointed the `~/dcachearea` symlink so analysis code kept working transparently. Upload path: scratchdisk first, then a Rucio replication rule. |
| 2026-06-09 | `hijing_overlay_r17618_grid_reprocessing.md` | Reprocessed all 6 pT slices of the Pythia fullsim HIJING overlay with corrected r17618 AODs (beamspot-z fix, clone of r17044) and replaced the old wrong-vertex-z NTUPs with the new skimmed output. |
| 2026-06-04 | `PR61_fix.md` | Changes needed for PR #61 to pass CodeRabbit review and the `check-skills` CI: SKILL.md frontmatter must carry only `name` + `description`, with `description` starting "Use when" and describing triggering conditions only. |
| 2026-06-03 | `pipeline_optimization_pythia.md` | Reviewed/improved the two Pythia pipelines (`pipeline_pythia_truth.sh` generator-level baseline, `pipeline_pythia_fullsim_overlay.sh` detector sim + HIJING overlay) against the 6 quality dimensions. Truth is the reference for validating fullsim overlay. |
| 2026-06-03 | `pipeline_optimization_powheg.md` | Reviewed/improved the two POWHEG fullsim pipelines (`..._single_muon.sh`, `..._mixed_pairs.sh`) against the 6 quality dimensions. NLO b-bbar dimuon predictions. Note: POWHEG fullsim is now obsolete; POWHEG truth survives only as the NLO template. |
| 2026-06-03 | `pipeline_optimization_pbpb.md` | Reviewed/improved the two PbPb pipelines (`pipeline_pbpb_trig_eff.sh`, `pipeline_pbpb_crossx.sh`) against the 6 quality dimensions. The two are tightly coupled — trig-eff outputs feed the crossx pipeline. |
| 2026-06-03 | `ntuple_processing_mode_cleanup.md` | Cleaned up NTuple-processing and RDF hist-filling mode logic: all 3 PbPb years run crossx with `trigger_mode=1` via one clearly-named boolean (not gated on `run_year==24`); trigger-efficiency vs nominal governed by a single flag; shell/.sub scripts for every mode × year. Inventory of `trigger_mode`, `resonance_cut_mode`, `trigger_effcy_calc`, `mindR_trig`, `output_single_muon_tree`. |
| 2026-06-03 | `grid_monitor_data_infra.md` | Grid/data infrastructure for the May 2026 skim: BigPanDA task polling, rucio download, hadd with recursive + chunked fallbacks, entry-count validation, `data_{prefix}_part{N}.root` naming, and automatic NTuple-processing code updates (`file_batch_max`, `.sub` queue counts) when datasets split into chunks. |
| 2026-05-19 | `rucio_download_investigation.md` | Why `rucio download` failed with `[Errno 5] Input/output error` on Python stdlib files under CVMFS while `list-dids`/`whoami`/ROOT/hadd all worked. Blocked the May 2026 skim downloads in `grid_monitor.sh`. (See also: exhausted GPFS quota makes rucio download fail on every RSE in <1 s.) |
| 2026-05-19 | `mu4_trig_effcy_investigation.md` | Trigger-efficiency plot directory structure under `plots/trig_effcy_plots/`, `isBNL` toggle for TrigEffPlotter (was Mac-only paths), and review of the Pipeline 2/3 physics: role-swap summing (mu1tag/mu2tag always combined, never separate), cross-term definition, and the 2mu4-vs-mu4 distinction (2mu4 has no cross term: ε₁·ε₂·ε_ΔR). |
| 2026-05-15 | `skim_discrepancy_investigation.md` | Why the old PbPb23 skim (R24, Apr 2025) had ~108M events and the new one (R25, Apr 2026) only ~10.4M, from the same 398M-event input. Compares AthAnalysis 24.2.40 vs 25.2.89, PanDA task configs, and `--nGBPerJob MAX --mergeOutput` XRootD remote I/O vs default local staging. |
| 2026-05-15 | `hijing_overlay_debug_summaries.md` | Two Apr 2026 investigations on Pythia 5.36 TeV pp hQCD DiMu overlaid on HIJING b<5 fm: (1) AOD truth-container survey — no Pythia-signal-only flat container exists; `TruthParticles` merges Pythia + HIJING, `TruthEvents`/`TruthPileupEvents` are link accessors, not flat containers. (2) Second debug thread; HIJING barcodes at bc > 200000. |
| 2026-05-15 | `event_selection_banana_cut_comparison.md` | Two procedures for the PbPb ZDC–FCal "banana" cut compared, slide by slide. Nominal = per-0.1-TeV-FCal-slice two-pass Gaussian fits: main band cut at μ+5σ, pileup/out-of-time band at μ_bg−3σ_bg for FCal E_T > 2 TeV, final cut = max of the two, with wider-bin retry and TGraph extrapolation where the pileup fit fails. |
| 2026-05-15 | `agent-infrastructure-gaps.md` | Domain knowledge needed for effective agent review but absent from repo docs / auto-memory, listed as numbered gaps to fill as the analysis matures (ATLAS publication plotting standards, systematic-uncertainty framework, ...). |
